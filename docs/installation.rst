============
Installation
============

At the command line::

    $ easy_install graphene-custom-directives

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv graphene-custom-directives
    $ pip install graphene-custom-directives
